<Window x:Class="CarDelershipWPF.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование" Height="200" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#2a2a2a">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="lblPrompt" Grid.Row="0" Text="Введите новое название:" 
                   Foreground="#FFC107" FontSize="14" Margin="0,0,0,15"/>

        <TextBox x:Name="txtInput" Grid.Row="1" Height="35" FontSize="14"
                 Background="#333" Foreground="White" BorderBrush="#555" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button x:Name="btnOk" Content="Сохранить" Width="100" Height="35"
                    Background="#FFC107" Foreground="Black" FontWeight="Bold"
                    Click="BtnOk_Click" Margin="0,0,10,0" Cursor="Hand"/>
            <Button x:Name="btnCancel" Content="Отмена" Width="100" Height="35"
                    Background="#333" Foreground="#FFC107" FontWeight="Bold"
                    Click="BtnCancel_Click" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>