<Window x:Class="CarDelershipWPF.Pages.ProductEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование товара" Height="650" Width="700"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#2a2a2a">

    <Window.Resources>
        <!-- Стили (оставляем без изменений) -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                          Background="#333333"
                                          Foreground="White"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="border" 
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="#555555"
                                                BorderThickness="1"
                                                CornerRadius="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition/>
                                                    <ColumnDefinition Width="25"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" 
                                                           Text="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                           Foreground="White"
                                                           VerticalAlignment="Center"
                                                           Margin="5,0,0,0"/>
                                                <Path Grid.Column="1" 
                                                      Data="M0,0 L5,5 L10,0" 
                                                      Fill="White"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      Margin="0,2,0,0"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#404040"/>
                                                <Setter TargetName="border" Property="BorderBrush" Value="#FFC107"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <Popup x:Name="Popup" 
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid x:Name="DropDown" 
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" 
                                            Background="#333333"
                                            BorderBrush="#555555"
                                            BorderThickness="1"
                                            CornerRadius="4">
                                        <ScrollViewer CanContentScroll="True">
                                            <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Left" 
                                              VerticalAlignment="Center"
                                              Margin="5,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFC107"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFC107"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="5,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#FFC107"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFC107"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <!-- Стиль для красной кнопки удаления -->
        <Style x:Key="DeleteButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#C82333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Редактирование товара" 
                       FontSize="20" FontWeight="Bold" Foreground="#FFC107" 
                       HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <!-- Изображение -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Фото:" Margin="5"/>
            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="5">
                <Image x:Name="imgPreview" Width="80" Height="60" Stretch="Uniform" 
                       Source="/Images/no_image.jpg" Margin="0,0,10,0"/>
                <Button x:Name="btnSelectImage" Content="📁 Выбрать фото" 
                        Background="#FFC107" Foreground="Black" FontWeight="Bold"
                        Click="BtnSelectImage_Click" Width="120" Height="35"/>
            </StackPanel>

            <!-- Название -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Название:" Margin="5"/>
            <TextBox x:Name="txtName" Grid.Row="2" Grid.Column="1" Margin="5" MaxLength="100"/>

            <!-- Модель -->
            <TextBlock Grid.Row="3" Grid.Column="0" Text="Модель:" Margin="5"/>
            <ComboBox x:Name="cmbModel" Grid.Row="3" Grid.Column="1" Margin="5"
                      DisplayMemberPath="Name" SelectedValuePath="Model_Id"/>

            <!-- Цвет -->
            <TextBlock Grid.Row="4" Grid.Column="0" Text="Цвет:" Margin="5"/>
            <ComboBox x:Name="cmbColor" Grid.Row="4" Grid.Column="1" Margin="5"
                      DisplayMemberPath="Name" SelectedValuePath="Color_Id"/>

            <!-- Цена -->
            <TextBlock Grid.Row="5" Grid.Column="0" Text="Цена:" Margin="5"/>
            <TextBox x:Name="txtPrice" Grid.Row="5" Grid.Column="1" Margin="5"
                     MaxLength="15"
                     PreviewTextInput="NumberOnly_PreviewTextInput"
                     PreviewKeyDown="Number_PreviewKeyDown"/>

            <!-- Цена со скидкой -->
            <TextBlock Grid.Row="6" Grid.Column="0" Text="Цена со скидкой:" Margin="5"/>
            <TextBox x:Name="txtDiscountPrice" Grid.Row="6" Grid.Column="1" Margin="5"
                     MaxLength="15"
                     PreviewTextInput="NumberOnly_PreviewTextInput"
                     PreviewKeyDown="Number_PreviewKeyDown"/>

            <!-- Количество -->
            <TextBlock Grid.Row="7" Grid.Column="0" Text="Количество:" Margin="5"/>
            <TextBox x:Name="txtQuantity" Grid.Row="7" Grid.Column="1" Margin="5"
                     MaxLength="6"
                     PreviewTextInput="IntegerOnly_PreviewTextInput"
                     PreviewKeyDown="Number_PreviewKeyDown"/>

            <!-- Год выпуска -->
            <TextBlock Grid.Row="8" Grid.Column="0" Text="Год выпуска:" Margin="5"/>
            <TextBox x:Name="txtYear" Grid.Row="8" Grid.Column="1" Margin="5"
                     MaxLength="4"
                     PreviewTextInput="IntegerOnly_PreviewTextInput"
                     PreviewKeyDown="Number_PreviewKeyDown"/>

            <!-- Статус -->
            <TextBlock Grid.Row="9" Grid.Column="0" Text="Статус:" Margin="5"/>
            <ComboBox x:Name="cmbStatus" Grid.Row="9" Grid.Column="1" Margin="5"/>

            <!-- Кнопки -->
            <StackPanel Grid.Row="10" Grid.ColumnSpan="2" Orientation="Horizontal" 
                        HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button x:Name="btnSave" Content="💾 Сохранить" Width="120" Height="40"
                        Background="#FFC107" Foreground="Black" FontWeight="Bold"
                        Click="BtnSave_Click" Margin="0,0,10,0"/>
                <Button x:Name="btnDelete" Content="🗑 Удалить" Width="120" Height="40"
                        Style="{StaticResource DeleteButtonStyle}"
                        Click="BtnDelete_Click" Margin="0,0,10,0"
                        Visibility="{Binding CanDelete, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Collapsed}"/>
                <Button x:Name="btnCancel" Content="❌ Отмена" Width="120" Height="40"
                        Background="#333" Foreground="#FFC107" FontWeight="Bold"
                        Click="BtnCancel_Click"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Window>